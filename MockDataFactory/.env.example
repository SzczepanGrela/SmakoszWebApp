# MockDataFactory Environment Configuration (PostgreSQL)

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mockdatadb
DB_USER=postgres
DB_PASSWORD=your_password_here

# Note: Create the database first with:
# createdb -U postgres mockdatadb
