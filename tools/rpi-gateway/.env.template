# Required
RPI_GATEWAY_API_TOKEN=your-secret-token-here
RPI_GATEWAY_GPU_WORKER_MAC=AA:BB:CC:DD:EE:FF

# Optional (defaults shown)
RPI_GATEWAY_PORT=5000

# Homelab API - used to start gpu-worker container after WoL
RPI_GATEWAY_HOMELAB_API_URL=http://<TAILSCALE_HOMELAB_IP>:5001
RPI_GATEWAY_HOMELAB_API_TOKEN=your-homelab-api-token
