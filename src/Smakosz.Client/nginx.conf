server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|wasm|dll|dat|blat|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Disable caching for SPA entry point and boot manifest
    location = /index.html {
        expires -1;
        add_header Cache-Control "no-store, no-cache";
    }

    location = /service-worker-assets.js {
        expires -1;
        add_header Cache-Control "no-store, no-cache";
    }

    # Disable caching for appsettings (overridden at runtime via volume mount)
    location ~* appsettings.*\.json$ {
        expires -1;
        add_header Cache-Control "no-cache";
    }
}
